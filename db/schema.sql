CREATE TABLE pets (
	id INTEGER NOT NULL, 
	nickname VARCHAR(64), 
	age INTEGER, 
	PRIMARY KEY (id)
);
CREATE TABLE foods (
	id INTEGER NOT NULL, 
	name VARCHAR(64), 
	cuisine VARCHAR(64), 
	PRIMARY KEY (id)
);
